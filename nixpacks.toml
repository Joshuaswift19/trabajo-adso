[phases.setup]
nixPkgs = ['php83', 'php83Extensions.pdo_pgsql', 'php83Packages.composer', 'nginx', 'nodejs_18', 'npm-9_x']

[phases.install]
cmds = ['composer install --ignore-platform-reqs']

[phases.build]
cmds = ['mkdir -p /var/log/nginx', 'mkdir -p /var/cache/nginx']